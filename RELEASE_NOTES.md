# 🚀 NexusCoder Studio — Release Notes v1.0.19 (Custom Installer, Silent Update & Performance)

**วันที่อัปเดต:** 24 สิงหาคม 2026  
**เวอร์ชัน:** `1.0.19`  
**สถานะการ Build:** ✅ สำเร็จ (Windows x64 — Custom Installer + Portable Executable, เซ็นใบรับรองครบ)

---

## 🛠️ รายการปรับปรุงและแก้บั๊กใน v1.0.19

### 1. 📥 หน้าติดตั้งใหม่ทั้งหมด เป็น UI ของ NexusCoder เอง (Custom Installer)
* **เลิกใช้ตัวช่วยติดตั้งของ NSIS/Windows** เปลี่ยนมาเป็นหน้าต่างติดตั้งสไตล์ Liquid Glass ที่ออกแบบเอง ชุดสีและฟอนต์เดียวกับตัวโปรแกรม รองรับภาษาไทยเต็มรูปแบบ
* **ฟีเจอร์ในหน้าติดตั้ง:** เลือกโฟลเดอร์ปลายทาง, ติ๊กสร้างไอคอนเดสก์ท็อป, เพิ่มลงเมนู Start, เปิดโปรแกรมอัตโนมัติหลังติดตั้งเสร็จ พร้อม Progress Bar ที่รายงานจำนวนไฟล์จริงระหว่างคัดลอก
* **หน้าถอนการติดตั้งของตัวเอง:** เรียกจาก Apps & features ของ Windows แล้วได้หน้าจอ UI เดียวกัน ไม่ใช่หน้าต่างมาตรฐานของระบบ
* **ลงทะเบียนกับ Windows ครบถ้วน:** สร้าง Shortcut ด้วย WScript.Shell และเขียนรายการถอนการติดตั้งใน Registry (`DisplayName`, `DisplayVersion`, `Publisher`, `EstimatedSize`) โปรแกรมจึงปรากฏในหน้า Apps & features ตามปกติ
* **สถาปัตยกรรม:** ไบนารีเดียวทำงานสองโหมด — ถ้าไฟล์ชื่อมีคำว่า `Setup` จะเข้าโหมดตัวติดตั้ง ถ้าไม่ใช่จะรันเป็นแอปแบบพกพา จึงไม่ต้องแพ็ก Electron runtime ซ้ำสองชุด **ขนาดไฟล์ลดจาก ~109 MB เหลือ ~92 MB**
* ไม่ต้องใช้สิทธิ์ Administrator (ติดตั้งลง `%LOCALAPPDATA%\Programs\NexusCoder`) และไม่พึ่ง native module ใด ๆ

### 2. 🔄 กดอัปเดตแล้วติดตั้งและรีสตาร์ทให้เอง (Silent Update)
* **ปัญหาเดิม:** กด "อัปเดตและรีสตาร์ท" แล้วโปรแกรมกลับเด้งหน้าตัวติดตั้งขึ้นมาให้กดต่อเอง แถมไม่เปิดโปรแกรมกลับมาให้
* **แก้ไข:** ตัวอัปเดตเรียกไฟล์ติดตั้งด้วยโหมด `--silent --install-dir=<โฟลเดอร์ปัจจุบัน>` ติดตั้งทับให้เงียบ ๆ แล้ว **เปิดโปรแกรมกลับมาให้อัตโนมัติ** ผู้ใช้เห็นแค่โปรแกรมปิดแล้วเปิดใหม่เป็นเวอร์ชันใหม่
* ติดตั้งทับโฟลเดอร์เดิมเสมอ ตำแหน่งติดตั้งที่ผู้ใช้เลือกไว้ตอนแรกจึงไม่ถูกย้าย
* ซ่อนหน้าต่าง Console สีดำที่เคยแวบขึ้นมาระหว่างอัปเดต และมีรอบตรวจสอบว่าตัวติดตั้งปิดสนิทก่อนเปิดโปรแกรมใหม่ ป้องกันการเปิดไบนารีที่ยังเขียนไม่เสร็จ

### 3. 🧩 พับ/ซ่อนแผงเค้าโครงหน้าได้ทุกคอลัมน์ (Foldable Panel Chrome)
* เพิ่ม **แถบหัวแผง** ให้ทุกคอลัมน์ (Explorer / Code Editor / AI Chat) ประกอบด้วยที่จับลาก, ไอคอน, ชื่อแผง และปุ่มพับเก็บ
* เดิมมีเพียงแผง Chat ที่พับได้จากในตัวเอง ส่วน Explorer และ Editor ต้องสั่งพับจากเมนู Layout บน Header เท่านั้น
* พับแล้วยุบเป็นแถบไอคอนแนวตั้ง คลิกเพื่อกางกลับ และจดจำสถานะไว้ใน `localStorage` เช่นเดิม

### 4. 🖱️ แก้บั๊กลากย้ายตำแหน่งแผงไม่ติด (Drag-to-Reorder)
* **สาเหตุ:** ระบบเดิมใช้ HTML5 Drag-and-Drop ซึ่งถูก Monaco Editor, Terminal และฟอร์มแชต (ที่เรียก `stopPropagation()` บน `onDragOver`) ดูดอีเวนต์ไป ทำให้การลากหลุดกลางทางเป็นประจำ
* **แก้ไข:** เปลี่ยนมาใช้ **Pointer Events + `setPointerCapture`** ทุกอีเวนต์การเคลื่อนเมาส์จึงวิ่งกลับมาที่ที่จับลากเสมอ ไม่ว่าเคอร์เซอร์จะลากผ่านองค์ประกอบใด
* แก้ลำดับการจัดเรียงใน `movePanel` — เดิมคำนวณตำแหน่งแทรกจาก index ก่อนลบรายการ ทำให้ลากไปทางขวาแล้วเหมือนไม่มีอะไรเกิดขึ้น
* แก้ `ResizeHandle` ให้อ่านสถานะการลากจาก ref แทน state จึงไม่ทิ้งพิกเซลช่วงแรกของการลากเร็ว ๆ อีกต่อไป
* ตัวแบ่งขนาดจะแสดงเฉพาะระหว่างแผงที่เปิดอยู่จริง และปรับความกว้างคอลัมน์ที่กำหนดขนาดได้จริง (เดิมลากฝั่ง Editor ซึ่งเป็น `flex-1` แล้วไม่ขยับ)

### 5. ⚡ แก้อาการแล็คตอน AI พิมพ์ และตอนผู้ใช้พิมพ์ (Streaming Performance)
* **แยก `AgentStreamContext` ออกจาก Context หลัก:** สถานะที่อัปเดตถี่ (token stream, progress) ไม่ทำให้ Editor, Sidebar และ Header re-render ตามทุกตัวอักษรอีกต่อไป พร้อม `useMemo` ค่า Context ทั้งสองชุดและตรึง identity ของฟังก์ชัน action
* **รวม Throttle ของ content/reasoning เป็นชุดเดียว** แล้ว flush บน `requestAnimationFrame` ทุก 80ms (เดิมใช้ 2 timer แย่งกันจนอีกฝั่งถูกหน่วง)
* **แยกกล่องพิมพ์เป็น `ChatComposer`:** ข้อความที่กำลังพิมพ์เก็บอยู่ใน state ของกล่องเอง การพิมพ์จึงไม่ re-render ประวัติแชตทั้งหมด — เป็นตัวแก้อาการ "พิมพ์แล้วหนืด" โดยตรง
* **`StreamingMessage`:** ข้อความที่กำลังไหลจะแสดงเป็นข้อความธรรมดาระหว่างสตรีม (การ parse Markdown ของเอกสารที่ยาวขึ้นเรื่อย ๆ สิบกว่าครั้งต่อวินาทีคือตัวกิน Main Thread) แล้วจึงเรนเดอร์เป็น Markdown เต็มรูปแบบเมื่อจบสตรีม

### 6. 🤖 แก้ปัญหา AI ทิ้งงานกลางคัน (Auto-Continue)
* เดิม Agent Engine ถือว่า "turn ที่ไม่เรียกเครื่องมือ = งานเสร็จ" ทันที แต่โมเดลมักบรรยายว่า *"ต่อไปผมจะแก้ไฟล์ config..."* แล้วหยุด ทำให้ผู้ใช้ต้องพิมพ์สั่งต่อเองตลอด
* เพิ่ม `detectUnfinishedWork()` ตรวจ 2 เงื่อนไข:
  * แผนงานใน `update_plan` ยังมีขั้นตอนที่สถานะไม่ใช่ `completed`
  * ข้อความสุดท้ายประกาศว่าจะทำอะไรต่อแต่ยังไม่ได้ลงมือ (จับรูปประโยคทั้งภาษาอังกฤษและภาษาไทย)
* หากเข้าเงื่อนไข ระบบจะ **สั่งให้ทำงานต่อเองอัตโนมัติ** สูงสุด 3 ครั้งต่อรอบ (รีเซ็ตโควตาทุกครั้งที่มีความคืบหน้าจริง) พร้อมแสดงสถานะ `Auto-continuing (n/3)` ให้ผู้ใช้เห็น
* ทำงานเฉพาะโหมด **Agent** และ **System** เท่านั้น ไม่รบกวนโหมด Ask และ Plan ที่ตอบแล้วจบตามธรรมชาติ

---

## ⚠️ หมายเหตุสำหรับการอัปเดตจากเวอร์ชันเก่า

ผู้ใช้ที่ติดตั้ง **v1.0.16 หรือเก่ากว่า** ผ่านตัวติดตั้ง NSIS เดิม แนะนำให้ถอนการติดตั้งของเก่าออกก่อนแล้วติดตั้ง v1.0.19 ใหม่ เพื่อไม่ให้มีรายการซ้ำในหน้า Apps & features (ระบบติดตั้งเปลี่ยนจาก NSIS มาเป็นตัวติดตั้งของ NexusCoder เอง) — ประวัติแชตและการตั้งค่าอยู่ในโฟลเดอร์ผู้ใช้ จึงไม่หายไปกับการถอนการติดตั้ง

---

## 📦 ไฟล์ติดตั้งและไบนารีที่พร้อมใช้งาน (Generated Release Artifacts)

| ประเภทไฟล์ | ชื่อไฟล์ | ขนาด | ลิงก์ไฟล์ |
| :--- | :--- | :--- | :--- |
| **Windows Installer** | `NexusCoder Setup 1.0.19.exe` | ~92 MB | [เปิดไฟล์ติดตั้ง](file:///d:/Project/Project_Program/dist-exe/NexusCoder%20Setup%201.0.19.exe) |
| **Portable Executable** | `NexusCoder 1.0.19.exe` | ~92 MB | [เปิดตัวพกพา](file:///d:/Project/Project_Program/dist-exe/NexusCoder%201.0.19.exe) |
| **Unpacked Folder** | `dist-exe/win-unpacked/` | — | [เปิดโฟลเดอร์](file:///d:/Project/Project_Program/dist-exe/win-unpacked) |
