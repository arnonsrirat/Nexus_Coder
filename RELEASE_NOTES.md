# 🚀 NexusCoder Studio — Release Notes v1.0.25 (Composer Lockup Fix & Auto-Continue Cleanup)

**วันที่อัปเดต:** 24 สิงหาคม 2026  
**เวอร์ชัน:** `1.0.25`  
**สถานะการ Build:** ✅ สำเร็จ (Windows x64 — Custom Installer + Portable Executable, เซ็นใบรับรองครบ)

---

## 🛠️ รายการปรับปรุงและแก้บั๊กใน v1.0.25

### 1. ⌨️ แก้ปุ่มส่ง/แนบไฟล์ค้างกดไม่ได้ถาวรหลังล้าง/สลับแชท
* **สาเหตุ:** ตัวแปร `isAgentBusy` (คุมว่าปุ่มส่งข้อความ/แนบไฟล์กดได้ไหม) คำนวณจากสถานะ `agentStatus` แต่ 3 จุดที่ควรรีเซ็ตสถานะกลับเป็นปกติ — **ล้างแชท (Clear Chat), สลับไปแชทอื่น, สร้างแชทใหม่** — รีเซ็ตแค่ตัวแปรความคืบหน้า (`agentProgress.isBusy`) แต่ไม่เคยรีเซ็ต `agentStatus` เลย
* **ผลกระทบก่อนแก้:** ถ้า `agentStatus` ค้างอยู่ที่ "กำลังทำงาน" ตอนกดล้าง/สลับแชทพอดี (เช่น ล้างแชทระหว่าง AI กำลังทำงาน หรือค้างจากบั๊กอื่นก่อนหน้า) ปุ่มส่ง/แนบไฟล์จะถูก**ปิดใช้งานถาวร**โดยไม่มีอะไรบอกเหตุผล พิมพ์ได้แต่กดส่งไม่ได้เลย
* **แก้ไข:** ทั้ง 3 จุดตอนนี้รีเซ็ต `agentStatus` กลับเป็นปกติด้วยแล้ว

### 2. 🧹 เอาระบบเดาข้อความ "งานยังไม่เสร็จ" ออกทั้งหมด (แก้ปัญหาตอบจบแล้วโดนสั่งทำต่อ ทั้งไทยและอังกฤษ)
* การจำกัดขอบเขต regex ให้แคบลงในเวอร์ชันก่อน (v1.0.23) ช่วยลดปัญหาภาษาไทยได้มาก แต่ยังมีการเดาผิดแบบเดียวกันเกิดขึ้นในภาษาอังกฤษด้วย — ประโยคสรุปปิดท้ายที่เขียนดีตามธรรมชาติ ยังบังเอิญไปตรงกับรูปแบบที่ระบบใช้เดาได้อยู่ดี
* **การตัดสินใจ:** การเดาจากข้อความไม่น่าเชื่อถือโดยธรรมชาติ ตัดออกทั้งหมด เหลือแค่การเช็คที่แม่นยำจริง — สถานะ plan step ที่ยังไม่เสร็จ (ข้อมูลจริงจาก AI เอง) ผนวกกับสัญญาณทางเทคนิคที่แน่นอนซึ่งมีอยู่แล้ว (`finish_reason: length`, คำตอบว่างเปล่า) ปัญหา "ตอบจบแล้วโดนสั่งทำต่อจนงง" ควรหมดไปทั้ง 2 ภาษาแล้ว

---

## 📄 สรุปจาก v1.0.24 (Silent Update: Hidden Launch & Reliable Completion)

* แก้ระบบอัปเดตค้างหน้าต่าง Command Prompt แบบถึงราก — ไฟล์ที่ดาวน์โหลดเป็น NSIS self-extracting stub ที่ปิดตัวเร็วกว่างานติดตั้งจริงจะเสร็จ เปลี่ยนมาใช้ `WScript.Shell.Run` ซ่อนหน้าต่างแบบรับประกัน + รอไฟล์ marker ยืนยันแทนการเดาจาก process name

รายละเอียดเต็มของ v1.0.24 ดูได้จากประวัติ git หรือ commit `chore: bump version to 1.0.24, release notes for silent-update root fix`

---

## 🛠️ (เดิม) รายการปรับปรุงและแก้บั๊กใน v1.0.24

### 1. 🪟 แก้ระบบอัปเดตค้างหน้าต่าง Command Prompt แบบถึงราก (รอบที่ 2)
* **สิ่งที่พบใหม่:** ไฟล์ที่ดาวน์โหลดมาเป็น **NSIS self-extracting stub** ไม่ใช่ตัวติดตั้งจริง — มันแค่แตกไฟล์แอปทั้งชุดไปไว้ที่โฟลเดอร์ temp แล้วรันจากตรงนั้น ตัว stub เองจึง**ปิดตัวได้เร็วกว่างานติดตั้งจริงจะเสร็จมาก** นี่คือสาเหตุที่การแก้รอบก่อน (รอด้วยชื่อ process ของไฟล์ที่ดาวน์โหลด) ไม่เวิร์ก — process นั้นหายไปเร็วเกินไป ทำให้ทั้งเปิดโปรแกรมใหม่ทับไฟล์ที่ยังเขียนไม่เสร็จ หรือค้างรอไม่มีวันจบ
* **แก้ไข 2 จุด:**
  1. เปลี่ยนวิธีเรียกตัวติดตั้งเป็น `WScript.Shell.Run` แบบ window style 0 (ซ่อนหน้าต่างแบบรับประกัน 100% ไม่ว่าโปรแกรมนั้นจะเป็นชนิดไหน) แทนการรันผ่าน cmd.exe ตรง ๆ ซึ่งซ่อนได้แค่หน้าต่างของ cmd เองเท่านั้น ไม่ได้ซ่อนหน้าต่างของโปรแกรมลูกที่มันเรียก — ทดสอบยืนยันการทำงานจริงก่อนปล่อยแล้ว
  2. ให้ตัวติดตั้ง (`electron/installer.cjs`) **เขียนไฟล์ marker ยืนยันเมื่อติดตั้งเสร็จจริง** แทนที่จะให้สคริปต์อัปเดตคอยเดาจากชื่อ process ซึ่งไม่น่าเชื่อถือ — ยังมีเพดานสูงสุด 45 วินาทีกันค้างตลอดกาลเหมือนเดิม

---

## 📄 สรุปจาก v1.0.23 (Auto-Continue False-Positive & Silent Empty Reply Fix)

* แก้ AI ตอบจบสมบูรณ์แล้วดูเหมือน "แตกกลางทาง" — regex ตรวจจับงานค้างเข้าใจผิดคำไทยธรรมดาอย่าง "ต่อไป"/"กำลังจะ" ว่าเป็นการพูดค้างไว้
* แก้กรณีโมเดลตอบกลับมาว่างเปล่าแล้วเงียบหายไปเลย — ตอนนี้ retry อัตโนมัติและแจ้งเตือนถ้ายังว่างอยู่

รายละเอียดเต็มของ v1.0.23 ดูได้จากประวัติ git หรือ commit `chore: bump version to 1.0.23, release notes for auto-continue fix`

---

## 🛠️ (เดิม) รายการปรับปรุงและแก้บั๊กใน v1.0.23

### 1. 🗣️ แก้ AI ตอบจบสมบูรณ์แล้วดูเหมือน "แตกกลางทาง" (โดยเฉพาะคำตอบยาว ๆ ภาษาไทย)
* **สาเหตุ:** ระบบ auto-continue ที่ควรช่วยไม่ให้ AI ทิ้งงานกลางคัน มีฟังก์ชัน `detectUnfinishedWork()` ที่เช็คคำใน**ข้อความทั้งหมด**แบบไม่มีเงื่อนไขจำกัดตำแหน่งเลย — คำไทยธรรมดาอย่าง **"ต่อไป"** หรือ **"กำลังจะ"** ที่ปรากฏได้ตามธรรมชาติในคำตอบยาว ๆ (แม้จะเป็นแค่ประโยคอธิบายทั่วไป ไม่ใช่การพูดค้างไว้) ถูกตีความผิดว่า "งานยังไม่เสร็จ" ทันที
* **ทำไมสั้น ๆ ถึงไม่มีปัญหา แต่ยาวขึ้นนิดเดียวก็พัง:** คำตอบสั้นไม่ค่อยมีโอกาสบังเอิญมีคำพวกนี้ แต่คำตอบยาวขึ้นมีโอกาสสูงมากที่จะมีคำเหล่านี้แฝงอยู่ที่ไหนสักแห่ง แม้จะตอบจบสมบูรณ์แล้วก็ตาม
* **ผลกระทบก่อนแก้:** AI ตอบจบสมบูรณ์ (เห็นพิมพ์จบจริง) → ระบบเข้าใจผิดว่ายังไม่เสร็จ → สั่งให้ทำงานต่ออัตโนมัติทันทีทั้งที่ไม่มีใครขอ → AI งงเพราะงานเสร็จไปแล้ว ตอบสั้น/ว่างเปล่ากลับมา → ดูเหมือนกำลังตอบอยู่ดี ๆ ก็หายไป
* **แก้ไข:** จำกัดให้เช็คเฉพาะท้ายข้อความ (~220 ตัวอักษรสุดท้าย) เพราะการพูดค้างไว้จริงต้องเป็นสิ่งสุดท้ายที่พูด และ regex ภาษาไทยต้องมีคำกริยาการกระทำต่อท้ายชัดเจนแล้ว (เช่น "กำลังจะแก้..." ไม่ใช่แค่ "กำลังจะ" ลอย ๆ)

### 2. 🔇 แก้กรณีโมเดลตอบกลับมาว่างเปล่าแล้วเงียบหายไปเลย
* บางครั้งโมเดลตอบกลับมาโดยไม่มีข้อความอะไรเลย (content filter เงียบ ๆ, ปัญหาฝั่งผู้ให้บริการ) — เดิมระบบไม่มีอะไรถูกเพิ่มเข้าแชทเลย แล้วสรุปว่า "งานเสร็จแล้ว" ทันที สปินเนอร์หยุดหมุนโดยไม่มีอะไรปรากฏ
* **แก้ไข:** คำตอบว่างเปล่าจะสั่งให้ลองตอบใหม่อัตโนมัติทันที (สูงสุด 3 ครั้ง) ถ้าลองครบแล้วยังว่างอยู่ดี จะขึ้นการ์ดแดงแจ้งชัดเจนแทนที่จะเงียบหายไปเฉย ๆ

---

## 📄 สรุปจาก v1.0.22 (Chat-Vanishing Fix & Simpler Status UI)

* แก้แชทหายไปทันทีก่อน AI จะได้ตอบ (ข้อความผู้ใช้ตอนนี้ขึ้นทันทีที่กดส่ง ไม่ต้องรอเซิร์ฟเวอร์ยืนยัน)
* เอาระบบการ์ดแสดงสถานะ AI แบบซับซ้อนออก เปลี่ยนเป็นแบบเรียบง่าย (`AgentStatusPulse`)

รายละเอียดเต็มของ v1.0.22 ดูได้จากประวัติ git หรือ commit `chore: bump version to 1.0.22, release notes for chat-vanish fix`

---

## 🛠️ (เดิม) รายการปรับปรุงและแก้บั๊กใน v1.0.22

### 1. 💬 แก้แชทหายไปทันทีก่อน AI จะได้ตอบ
* **สาเหตุ:** ข้อความที่ผู้ใช้พิมพ์ **ไม่เคยถูกแสดงบนจอทันทีที่กดส่ง** — ระบบรอให้เซิร์ฟเวอร์ส่งข้อความกลับมายืนยันก่อน (ผ่าน event `message_added`) ถึงจะเอาไปแสดงผล ถ้า round-trip นี้สะดุดตรงไหนก็ตาม (เช่น เซิร์ฟเวอร์ปฏิเสธคำขอ, การเชื่อมต่อสะดุดเสี้ยววินาที) ข้อความที่พิมพ์ไปจะไม่ปรากฏบนจอเลยแม้แต่ครั้งเดียว — ดูเหมือนแชททั้งหน้าหายไปทันทีก่อนที่ AI จะได้มีโอกาสตอบด้วยซ้ำ
* **แก้ไข:** ให้ข้อความของผู้ใช้ขึ้นแสดงทันทีตอนกดส่ง (optimistic update) แล้วพอเซิร์ฟเวอร์ยืนยันกลับมาค่อยสลับเป็นข้อมูลจริงแทนที่ในตำแหน่งเดิม (ไม่ซ้ำกัน) — ต่อให้ round-trip มีปัญหา ข้อความที่พิมพ์ก็จะยังค้างอยู่บนจอให้เห็นเสมอ ไม่หายไปไหน

### 2. 🧹 เอาระบบ "การ์ดแสดงสถานะ AI" แบบซับซ้อนออก เปลี่ยนเป็นแบบเรียบง่าย
* การ์ดเดิม (`AgentThinkingCard`) มี timer นับเวลาของตัวเอง, breadcrumb 4 ขั้นตอน, เปอร์เซ็นต์ progress, stopwatch ฯลฯ — มีจุดที่ state อาจไม่ตรงกันได้เยอะ
* เปลี่ยนเป็น `AgentStatusPulse` ที่เรียบง่ายมาก มีแค่ไอคอนหมุน + ข้อความสถานะบรรทัดเดียว ลดพื้นที่ที่จะเกิดบั๊กลงไปมาก

---

## 📄 สรุปจาก v1.0.21 (Silent Update Freeze Fix)

### 1. 🪟 แก้ระบบอัปเดตค้างหน้าต่าง Command Prompt
* **สาเหตุที่ 1:** สคริปต์อัปเดตเดิมเรียกตัวติดตั้งด้วย `start /wait "" installer.exe ...` — คำสั่ง `start` จะเปิด process ใหม่แยกต่างหากเสมอ ต่อให้ cmd.exe ตัวแม่ถูกซ่อนหน้าต่างไว้แล้ว (`windowsHide: true`) แต่ process ใหม่ที่ `start` เปิดมีโอกาสได้หน้าต่าง console เป็นของตัวเอง นี่คือตัวที่ทำให้เห็นหน้าต่าง command prompt โผล่ขึ้นมา
* **แก้ไข:** เรียกตัวติดตั้งตรง ๆ ในบรรทัดเดียวแทนโดยไม่ผ่าน `start` (cmd.exe รอมันจบเองอยู่แล้วโดยธรรมชาติ ไม่ต้องพึ่ง `/wait`) จึงไม่มี process ใหม่ให้เปิดหน้าต่างขึ้นมาอีก
* **สาเหตุที่ 2:** โค้ดเดิมมีลูป `tasklist` คอยเช็คว่าตัวติดตั้งปิดตัวสนิทหรือยัง (เพราะ NSIS stub บางครั้งไม่ปิดตัวพร้อมกับ `start /wait`) แต่ลูปนี้ **ไม่มีเงื่อนไขหยุดเลย** — ถ้าตัวติดตั้งไม่ยอมปิดตัวสนิทด้วยเหตุผลใดก็ตาม ลูปจะวนตลอดไปและหน้าต่างค้างอยู่แบบนั้นไม่จบสิ้น ตรงกับอาการที่ผู้ใช้เจอ
* **แก้ไข:** ใส่เพดานสูงสุด 30 วินาทีให้ลูปนี้ ถ้าเกินก็เลิกรอแล้วรีสตาร์ทแอปต่อทันที แทนที่จะค้างรอไม่มีที่สิ้นสุด
* **เร็วขึ้นด้วย:** ตัดดีเลย์ `timeout` ที่ไม่จำเป็นออกจาก 3 จุดเหลือ 1 จุด (ดีเลย์ 1 วินาทีแรกเก็บไว้เพื่อให้แอปปิดตัวแบบ graceful ก่อนโดนบังคับปิด ป้องกันข้อมูลเขียนไม่ครบ) จากเดิมที่หน่วงเวลารวมกันเกือบ 4-5 วินาทีโดยไม่จำเป็น ตอนนี้กระบวนการอัปเดตทั้งหมดเร็วขึ้นชัดเจน

---

## 📄 สรุปจาก v1.0.20 (Silent Error Bugs, Theme Overhaul & Context Stability)

* แก้บั๊กใหญ่ที่ข้อความ error หายไปเงียบ ๆ (duplicate `case 'error'` ในสวิตช์เดียวกัน ทำให้โค้ดที่ถูกต้องกลายเป็น dead code)
* แก้บั๊กแบบเดียวกันที่ทำให้แจ้งเตือน "อัปเดตพร้อมติดตั้ง" ไม่เคยเด้งขึ้นมาเอง
* แก้ AI ตอบครึ่ง ๆ กลาง ๆ แล้วหยุด (max_tokens ชนงบ thinking, ไม่เช็ก finish_reason, รูปเก่าบวม context)
* แก้การ์ด Tool Call ค้างที่ "Running..." ตลอดไปและซ้ำซ้อนกัน
* ทำให้ธีมเปลี่ยนดีไซน์จริง ไม่ใช่แค่เปลี่ยนสีที่มองแทบไม่เห็น

รายละเอียดเต็มของ v1.0.20 ดูได้จากประวัติ git หรือ commit `docs: release notes for v1.0.20`

---

## 🛠️ (เดิม) รายการปรับปรุงและแก้บั๊กใน v1.0.20

### 1. 🔇 แก้บั๊กใหญ่: ข้อความ error หายไปเงียบ ๆ ไม่มีอะไรบอกผู้ใช้เลย
* **สาเหตุที่แท้จริง:** ในตัวจัดการ event ฝั่ง client (`AppContext.jsx`) มี `case 'error':` ซ้ำกัน **2 อัน** อยู่ในสวิตช์เดียวกัน — ตามหลัก JavaScript สวิตช์จะรันแค่ `case` แรกที่ตรงเท่านั้น ทำให้บล็อกที่สองกลายเป็น **โค้ดที่ไม่มีวันถูกรันเลย**
* บล็อกแรก (ที่รันจริง) เอาข้อความ error ไปใส่ใน `agentProgress.stepText` ซึ่งแสดงผลผ่านการ์ด "AI กำลังคิด" เท่านั้น แต่ในอัปเดตเดียวกันนั้นเองก็สั่งปิดการ์งนั้นทันที ข้อความ error จึงไม่เคยถูกเห็น
* บล็อกที่สอง (ที่ตายไปเฉย ๆ) คือโค้ดที่ถูกต้อง — push การ์ด error สีแดงเข้าแชทให้เห็นชัดเจน — แต่ไม่เคยถูกรัน
* **ผลกระทบก่อนแก้:** ถ้าเซิร์ฟเวอร์ปฏิเสธคำสั่งใหม่ (เช่น "Agent is already running" ตอน state ไม่ตรงกันหลังหลุดการเชื่อมต่อ, หรือส่งข้อความซ้อนกัน) ข้อความที่ผู้ใช้พิมพ์จะ **หายไปเงียบ ๆ โดยไม่มีคำอธิบายใด ๆ** — ตรงกับอาการ "ตอบๆ แล้วหาย" ที่ผู้ใช้เจอ
* **แก้ไข:** รวม 2 บล็อกเป็นอันเดียว ตอนนี้ error ทุกครั้งจะขึ้นเป็นการ์ดแดงในแชทเสมอ พร้อมข้อความเหตุผลชัดเจน

### 2. 🔔 แก้บั๊กแบบเดียวกัน: แจ้งเตือนอัปเดตพร้อมติดตั้งไม่เคยเด้งขึ้นมาเอง
* พบรูปแบบบั๊กเดิม (duplicate `case`) ซ้ำอีก 3 จุดในสวิตช์เดียวกัน: `update_progress`, `update_ready`, `update_error`
* บล็อกที่ตายไปของ `update_ready` มีคำสั่ง **เปิดหน้าต่างแจ้งเตือน "อัปเดตพร้อมติดตั้ง"** ซึ่งไม่เคยรันมาก่อนเลย — ผู้ใช้จึงไม่เคยเห็น popup เตือนหลังดาวน์โหลดอัปเดตเสร็จ ต้องเข้าไปเช็กเองในเมนู
* บล็อกที่ตายไปของ `update_progress` มีการตั้งสถานะเป็น "downloading" ที่หายไปเช่นกัน ทำให้แถบสถานะระหว่างดาวน์โหลดแสดงไม่ตรง
* **แก้ไข:** รวม logic ทั้งหมดเข้าบล็อกแรก ลบโค้ดที่ตายไปทิ้ง ตอนนี้แจ้งเตือนอัปเดตพร้อมติดตั้งจะเด้งขึ้นอัตโนมัติแล้ว

### 3. 🧵 แก้ AI ตอบครึ่ง ๆ กลาง ๆ แล้วหยุดไปเฉย ๆ (โดยเฉพาะหลังแนบรูป/วิดีโอ)
* **สาเหตุ #1 — `max_tokens` ชนกับงบ Thinking:** โค้ดเดิมตั้ง `max_tokens` ตายตัวที่ 8192 แต่ระดับ Reasoning "High" ใช้งบ thinking ถึง 16000 token (เกิน `max_tokens` เอง — คำขอไม่ถูกต้องตามข้อกำหนดของ Anthropic) และแม้แต่ระดับ "Medium" (8000) ก็แทบไม่เหลือที่ให้คำตอบจริงหลังหักงบ thinking ไปแล้ว → ปรับให้ `max_tokens` ขยายตามงบ thinking เสมอ
* **สาเหตุ #2 — ไม่เคยเช็ก `finish_reason`:** เวลาโมเดลถูกตัดกลางคันเพราะชน `max_tokens` (`finish_reason: "length"`) ระบบเดิมไม่รู้เลย ปฏิบัติเหมือนงานเสร็จสมบูรณ์แล้วเงียบไปดื้อ ๆ → เพิ่มการตรวจจับและสั่งให้ AI พูดต่อจากที่ค้างไว้โดยอัตโนมัติ
* **สาเหตุ #3 — รูปภาพเก่าถูกส่งซ้ำทุกเทิร์นตลอดไป:** พอแนบรูป ข้อมูล base64 (อาจหลาย MB) จะติดอยู่ใน conversation history และถูกส่งซ้ำไปกับทุกคำขอถัดไปตลอดทั้งเซสชัน ทำให้ prompt บวมเร็วมาก จนอาจโดนระบบบีบอัด "middle-out" ของ OpenRouter ตัดกลางบล็อก base64/JSON จนพังโครงสร้าง → ตัดรูปเก่าที่ผ่านเทิร์นไปแล้วออก เหลือเป็นข้อความ placeholder แทน เก็บเฉพาะไฟล์แนบล่าสุดที่ยังไม่ได้ตอบ

### 4. 🖱️ แก้การ์ด Tool Call ค้างที่ "Running..." ตลอดไป และซ้ำซ้อนกัน
* **สาเหตุ:** เวลา AI เรียกใช้เครื่องมือ (เช่น `run_command`) ระบบฝังการ์ด "Running..." ไว้ในข้อความของ AI ตั้งแต่แรก แต่ไม่เคยอัปเดตสถานะกลับเป็น Success/Error เมื่อรันเสร็จ — สร้างการ์ดใหม่แยกออกมาข้างล่างแทน ทำให้เห็นการ์ดซ้ำ (อันบนค้าง Running ตลอดกาล อันล่าง Success)
* มี `React.memo` comparator ใน `MessageItem.jsx` ที่เช็คแค่ "จำนวน" tool call ไม่เช็คว่าผลลัพธ์เปลี่ยนไหม แม้แก้ state ถูกต้องแล้ว การ์ดก็ยังไม่ re-render
* **แก้ไข:** ทั้งฝั่ง server และ client จะอัปเดตการ์ดเดิมที่ค้างอยู่แทนการสร้างใหม่ซ้ำ (มีผลทั้งตอนใช้งานสดและตอนโหลดแชตเก่ากลับมาดู) พร้อมแก้ memo comparator ให้เช็คผลลัพธ์จริง

### 5. 🎨 ธีมตอนนี้เปลี่ยนดีไซน์จริง ไม่ใช่แค่เปลี่ยนสีที่มองแทบไม่เห็น
* **สาเหตุเดิม:** ตัวแปรสีของธีม (`--color-bg-panel`, `--glass-border` ฯลฯ) ถูกนิยามไว้ถูกต้อง แต่มีแค่กรอบนอกสุดของแอปเท่านั้นที่ใช้ตัวแปรพวกนี้ ส่วนพาเนล/การ์ด/แถบต่าง ๆ ในแอปกว่า 260 จุด hardcode สี Tailwind (`bg-slate-900`, `border-slate-800` ฯลฯ) ตายตัว ไม่ตอบสนองต่อธีมที่เลือกเลย
* แก้ให้ทุก panel/card/border ที่ hardcode ไว้ทั่วทั้งแอปเปลี่ยนไปใช้สีและ blur ของธีมจริง ๆ (ครอบคลุมทั้งคลาสแบบมี opacity ต่อท้าย เช่น `border-slate-800/60` และสี cyan ทุกเฉด ไม่ใช่แค่คลาสตรงตัว)
* เพิ่ม **ลายเซ็นดีไซน์เฉพาะตัว** ต่อธีม แทนที่จะเป็นแค่สีต่างกัน: `liquid-glass` มุมโค้งมนมาก (bubbly), `aurora-glass` มุมโค้งกลาง ๆ, `obsidian-glass` มุมคมเกือบเหลี่ยม (แข็ง/พรีเมียมแบบหินออบซิเดียน) พร้อมเส้น accent บนขอบพาเนลหลัก, scrollbar และ glow effect ที่เปลี่ยนสีตามธีมด้วย

---

## 📦 ไฟล์ติดตั้งและไบนารีที่พร้อมใช้งาน (Generated Release Artifacts)

| ประเภทไฟล์ | ชื่อไฟล์ | ขนาด | ลิงก์ไฟล์ |
| :--- | :--- | :--- | :--- |
| **Windows Installer** | `NexusCoder Setup 1.0.25.exe` | ~92 MB | [เปิดไฟล์ติดตั้ง](file:///d:/Project/Project_Program/dist-exe/NexusCoder%20Setup%201.0.25.exe) |
| **Portable Executable** | `NexusCoder 1.0.25.exe` | ~92 MB | [เปิดตัวพกพา](file:///d:/Project/Project_Program/dist-exe/NexusCoder%201.0.25.exe) |
| **Unpacked Folder** | `dist-exe/win-unpacked/` | — | [เปิดโฟลเดอร์](file:///d:/Project/Project_Program/dist-exe/win-unpacked) |
